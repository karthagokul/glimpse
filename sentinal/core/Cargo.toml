[package]
name = "sentinel"
version = "0.1.0"
edition = "2021"

[dependencies]
plugin_api = { path = "../plugin_api" }
libloading = "0.8"

tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
